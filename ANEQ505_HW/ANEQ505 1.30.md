Friday work times outside of class command to start with alpine

During class times
```
sinteractive --reservation=aneq505 --time=01:00:00 --partition=amilan --nodes=1 --ntasks=2 --qos=normal
```

Outside of class
```
ainteractive
```

### Cont. Alpine 2.06


```
mkdir manifest
```
- The manifest directory, pairing up sequence with its file.


run 1
```
qiime tools import \--type "SampleData[PairedEndSequencesWithQuality]" \--input-format PairedEndFastqManifestPhred33V2 \--input-path manifest/manifest_run2.txt \--output-path demux/demux_run2.qza
```

run 2
```
qiime tools import \--type "SampleData[PairedEndSequencesWithQuality]" \--input-format PairedEndFastqManifestPhred33V2 \--input-path manifest/manifest_run3.txt \--output-path demux/demux_run3.qza
```

## Denoising
- Two runs for denoising.

run 2
``` 
```

run3
```

```

Merging reads
- Using a 10 sequence... in order to merge reads


Read depth per sample
- A way why we would change the sampling depth for what we wouldn't want to use

DADA2 Seq Files
- Mistook the table files with the seq files.
- Table files
qiime feature-table merge \--i-tables table_run2.qza \--i-tables table_run3.qza \--o-merged-table table.qza  
  
qiime feature-table summarize \--i-table table.qza \--o-visualization table.qzv \--m-sample-metadata-file ../metadata/metadata.txt

Seq files 
 qiime feature-table merge-seqs \--i-data seqs_run2.qza \--i-data seqs_run3.qza \--o-merged-data seqs.qza  
  
qiime feature-table tabulate-seqs \--i-data seqs.qza \--o-visualization seqs.qzv

-
 They ran, I just didn't get my table qzv 
- Went back for the table qzv, ran code
	- jaja si va

Creating a job
- 


